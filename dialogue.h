#pragma once
#include <string>

static void print_line(std::string line, int delay = 30);

static void print(std::string line, int delay = 30);

static void add_pause(int delay);

void show_title();

void show_opening_monologue();
